import { Route } from '@angular/router';
import { ProjectsViewComponent } from './projects-view.component';

export const projectsViewRoutes: Route[] = [
    {
        path: '',
        component: ProjectsViewComponent,
    },
];
